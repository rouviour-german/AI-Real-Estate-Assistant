/**
 * useAuth hook
 *
 * This is a simple re-export of the useAuth hook from AuthContext.
 * The actual implementation is in the AuthContext file.
 */

export { useAuth } from '@/contexts/AuthContext';
export type { AuthContextType } from '@/contexts/AuthContext';
